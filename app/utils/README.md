# utils

Shared utility functions with no domain knowledge.

## Files

| File | Purpose |
|---|---|
| `retry.py` | `retry_with_backoff()` — generic retry wrapper with exponential backoff. Used by LLM client, IMAP connections, and browser operations |
